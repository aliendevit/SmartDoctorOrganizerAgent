# Placeholder for local HF client wrapper. Replace with actual implementation if needed.
class HFClient:
    def __init__(self, model_name: str = ''):
        self.model_name = model_name
    def classify(self, text: str) -> str:
        return "unknown"
